import {makeStyles} from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
    root: {
        marginTop: 10
    },
    image_blog: {
        height: 340,
        display: 'block'
    },
    author: {
        color: '#111'
    }
}))

export default useStyles;